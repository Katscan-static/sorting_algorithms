#include <stdio.h>
#include "sort.h"
#include <stdlib.h>


void insertion_sort_list(listint_t **list)
{
	listint_t *node1;
	listint_t *node2;
	listint_t *hold;
	listint_t *hold1

	if (!list or !*list)
		return;

	node1 = *list;
	node2 = current->next;
	

	
}
